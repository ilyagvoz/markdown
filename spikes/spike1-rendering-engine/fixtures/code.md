# Code

Inline code should look like `let value = 42`.

```swift
struct Document {
    let title: String
    let body: String
}
```

```ts
type Renderer = {
  render(markdown: string): Promise<string>;
};
```
